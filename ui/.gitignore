# Electron build output
dist-electron/

# iOS 模拟器无签名构建产物（scripts/ios-sim.sh）
dist-ios/

# Local agent scratch
tmp/

# E2E cleanup sidecars (baseline/keeper claw snapshots)
e2e/.baseline-claws.json
e2e/.keeper-claws.json

# E2E 测试浏览器弹窗位置（本机专属，多屏时弹到副屏）
e2e/.window-position
