---
description: 手动加载 server 工作区指令，供未自动加载工作区 CLAUDE.md 的场景（worktree、teammate 等）
disable-model-invocation: true
---

读取以下文件，并将其内容作为 `server` 工作区的 instructions 严格遵循（效力等同于系统加载的 CLAUDE.md）。后续涉及 `server` 工作区的开发工作须受这些 instructions 约束：

1. `server/CLAUDE.md`
