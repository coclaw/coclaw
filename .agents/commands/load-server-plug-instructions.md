---
disable-model-invocation: true
---

读取以下文件，并将其内容作为 `server` 和 `plugins/openclaw`（含其父级 `plugins`）工作区的 instructions 严格遵循（效力等同于系统加载的 CLAUDE.md）。后续涉及这些工作区的开发工作须受对应 instructions 约束：

1. `server/CLAUDE.md`
2. `plugins/CLAUDE.md`
3. `plugins/openclaw/CLAUDE.md`
