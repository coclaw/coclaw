---
disable-model-invocation: true
---

读取以下文件，并将其内容作为 `plugins/openclaw` 工作区（含其父级 `plugins`）的 instructions 严格遵循（效力等同于系统加载的 CLAUDE.md）。后续涉及该工作区的开发工作须受这些 instructions 约束：

1. `plugins/CLAUDE.md`
2. `plugins/openclaw/CLAUDE.md`
