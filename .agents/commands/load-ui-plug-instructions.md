---
description: 手动加载 ui 与 plugins/openclaw 两个工作区指令，供未自动加载工作区 CLAUDE.md 的场景（worktree、teammate 等）
disable-model-invocation: true
---

读取以下文件，并将其内容作为 `ui` 和 `plugins/openclaw` 工作区的 instructions 严格遵循（效力等同于系统加载的 CLAUDE.md）。后续涉及这些工作区的开发工作须受对应 instructions 约束：

1. `ui/CLAUDE.md`
2. `plugins/openclaw/CLAUDE.md`
