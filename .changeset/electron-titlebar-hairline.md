---
"@coclaw/ui": patch
---

Add a hairline below the Electron custom titlebar so the chrome band reads as intentional next to the sidebar and content on wide desktop windows. Two further electron-scoped options (deeper chrome fill, native full-height sidebar) are kept as commented opt-ins.
