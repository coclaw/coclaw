---
"@coclaw/ui": patch
---

Make the "oauth" badge text searchable in the add-provider list. Typing "oauth" in the provider search now surfaces oauth-capable providers; previously it matched nothing, even though those providers visibly carry an "oauth" badge.
