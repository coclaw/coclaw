---
"@coclaw/ui": patch
---

Restyle the Windows title bar brand to align with the sidebar icon column (24px logo with its left edge at 16px matching the nav icons, 14px text) instead of the Microsoft title bar metrics: the bar sits directly above the sidebar, so at that distance the standard metrics read as almost-but-not-aligned.
