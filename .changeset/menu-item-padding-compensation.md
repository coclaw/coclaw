---
"@coclaw/ui": patch
---

Nudge popover menu item padding to `pl-4 pr-5` across the topic, agent, web-agent, main-list add, and slash-command menus. The lucide icon glyphs render narrower than their box, so the extra right padding keeps each row's whitespace visually balanced.
