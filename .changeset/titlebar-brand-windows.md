---
"@coclaw/ui": patch
---

Show the app logo + name on the left of the Electron custom title bar on Windows (following the Microsoft title bar design metrics), and hide the now-redundant sidebar brand row there, restoring the 8px top gap above the first nav item. macOS keeps the traffic-lights-only title bar and the sidebar brand, matching platform conventions.
