---
'@coclaw/ui': minor
---

Surface provider brand names (via a single getProviderName helper) in the add-provider dialog, API-key list, remove-confirm, and dashboard model tags; add brand-name search in the add-provider dialog. Display-only; provider id remains the source of truth.
