---
"@coclaw/ui": minor
---

Polish model-config provider badges: show an `oauth` capability badge on OAuth-capable providers in the add-provider list (api-key-only providers stay unbadged to reduce noise), and bump all model-config badges (oauth tag, source tag, add-list oauth tag) from `xs` to `sm` for legibility.
