---
"@coclaw/ui": patch
---

Rename the add-provider OAuth wording to action-based terms: the device code is now surfaced as a "verification code", and the two OAuth methods are named by what the user does ("authorize with a code" / "authorize in browser") instead of the asymmetric "code sign-in" vs "OAuth sign-in". Applied across all 12 locales.
