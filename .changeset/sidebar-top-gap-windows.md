---
"@coclaw/ui": patch
---

Add an 8px top gap above the sidebar's first nav group on the Windows desktop app, where the brand row is hidden, so the list isn't flush against the top edge. No effect on macOS/Linux/browsers, where the brand row already provides that spacing.
