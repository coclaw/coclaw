---
"@coclaw/ui": patch
---

Reduce the add-provider dialog body top padding (pt-3 → pt-2) so the authorization-code row's taller copy button no longer looks cramped against the top. The body wrapper is shared by every screen of the dialog, so all screens' top spacing tightens by one step.
