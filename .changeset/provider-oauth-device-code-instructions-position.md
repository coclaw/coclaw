---
"@coclaw/ui": patch
---

Move the "click the link below to authorize" instruction on the
account-authorization device-code screen to sit directly above the link
(after the authorization code) instead of at the top — previously the code
row sat between the instruction and the link it refers to, so "the link
below" pointed past the code.
